{
  "name": "memstrata-browser-extension",
  "version": "5.3.0",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.js",
    "watch": "node esbuild.config.js --watch",
    "zip": "node esbuild.config.js && node scripts/zip.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.279",
    "@types/jsdom": "^21.0.0",
    "esbuild": "^0.24.0",
    "jsdom": "^24.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
